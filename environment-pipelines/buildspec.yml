version: 0.2
env:
  variables:
#    COPILOT_VERSION: 1.32.1
#    DYFF_VERSION: 1.5.8
phases:
  install:
    commands:
      - echo "Install Phase"
  build:
    commands:
      - echo "Build Phase"
  post_build:
    commands:
      - echo "Post Build Phase"
artifacts:
  files:
#    - "infrastructure/*"
